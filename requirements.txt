# MetaWiki Framework - Dependencies
# Kernfunktionen: Nur Python Standard Library erforderlich (Python 3.10+)
# Keine externen Pakete für Import, Export, CLI oder Validierung benötigt.

# --- Optionale Abhängigkeiten ---

# KI-Übersetzung via Claude API (translate.py, metawiki_pipeline.py translate)
# Voraussetzung: ANTHROPIC_API_KEY Umgebungsvariable setzen
# anthropic>=0.40.0
